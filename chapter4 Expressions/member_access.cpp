/*
 * The arrow operator requires a pointer operand and yields
 * an lvalue. The dot operator yields an lvalue if the object
 * from which the member is fetched is an lvalue; otherwise
 * the result is an rvalue.
 */