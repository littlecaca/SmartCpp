#include <iostream>
#include <iterator>
#include <memory>

using namespace std;


int main(int argc, char const *argv[])
{
    istreambuf_iterator<char> it(std::cin);
    istream_iterator<int> it2(std::cin);
    // istrema
    return 0;
}
