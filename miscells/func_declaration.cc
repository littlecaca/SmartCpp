#include <iostream>
#include <list>
#include <sstream>
#include <iterator>

using namespace std;

int func(int f2());
int func2((double (2)));
int func3(double (2));

int main(int argc, char const *argv[])
{
    // list<int> data(istream_iterator<int>(datefile), istream_iterator<int>());
    cout << func2 << endl;
    return 0;
}

