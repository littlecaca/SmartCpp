template <typename  T>
int compare(const T &, const T &);

int main(int argc, char const *argv[])
{
    // compare("ii", "sadad");
    char s1[4], s2[4]; 
    compare("sad", "dfd");
    compare(s1, s2);

    "sdas";     // string literal has the type of const char[]
    return 0;
}
