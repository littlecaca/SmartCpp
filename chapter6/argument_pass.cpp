#include <iostream>

/*
 * When a parameter is a reference, we say that its corresponding argument is
 * "passed by reference" or that function is "called by reference". The opposite
 * operation is called "passed by value" or "called by value".
 * 
 * 
 * Reference prarmeters that are not changed inside a function should be references to const.
 */

int main(int argc, char const *argv[])
{
    
    return 0;
}
